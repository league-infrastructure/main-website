---
import { utilityLinks, mainNavigation, navCtas } from '../data/navigation';
import { siteSettings } from '../data/siteSettings';
---

<header class="site-header">
  <div class="site-header__top">
    <div class="container site-header__top-inner">
      <span class="site-header__tagline">{siteSettings.tagline}</span>
      <nav aria-label="Utility">
        <ul class="list-reset site-header__utility-list">
          {utilityLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </nav>
    </div>
  </div>
  <div class="container site-header__main">
    <a class="site-header__logo" href="/">{siteSettings.name}</a>
    <nav aria-label="Main navigation" class="site-header__nav">
      <ul class="list-reset site-header__nav-list">
        {mainNavigation.map((item) => (
          <li>
            <a href={item.href} target={item.external ? '_blank' : undefined} rel={item.external ? 'noreferrer' : undefined}>
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <div class="site-header__actions">
      {navCtas.map((cta) => (
        <a class={`button ${cta.label === 'Donate' ? 'button--primary' : 'button--secondary'}`} href={cta.href}>
          {cta.label}
        </a>
      ))}
    </div>
  </div>
</header>

<style>
  .site-header__top {
    background: var(--color-surface-alt);
    font-size: 14px;
    color: var(--color-muted);
  }

  .site-header__top-inner {
    display: flex;
    gap: var(--space-md);
    align-items: center;
    justify-content: space-between;
    padding-block: var(--space-sm);
  }

  .site-header__tagline {
    font-weight: 500;
    color: var(--color-primary);
  }

  .site-header__utility-list {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .site-header__utility-list a {
    color: var(--color-body);
    font-weight: 500;
  }

  .site-header__main {
    display: flex;
    align-items: center;
    gap: var(--space-xl);
    padding-block: var(--space-lg);
  }

  .site-header__logo {
    font-weight: 700;
    font-size: 20px;
    color: var(--color-text);
  }

  .site-header__nav {
    flex: 1;
  }

  .site-header__nav-list {
    display: flex;
    gap: var(--space-lg);
    flex-wrap: wrap;
  }

  .site-header__nav-list a {
    color: var(--color-text);
    font-weight: 500;
  }

  .site-header__actions {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  @media (max-width: 980px) {
    .site-header__main {
      flex-direction: column;
      align-items: stretch;
      gap: var(--space-md);
    }

    .site-header__actions {
      justify-content: center;
    }

    .site-header__nav-list {
      justify-content: center;
    }
  }
</style>
