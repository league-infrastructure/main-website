<style is:global>
  .policy-hero__content {
    display: grid;
    gap: var(--space-md);
    max-width: 720px;
  }

  .policy-intro {
    display: grid;
    gap: var(--space-md);
    max-width: 720px;
  }

  .policy-checklist {
    display: grid;
    gap: var(--space-lg);
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  }

  .policy-checklist__card {
    display: grid;
    gap: var(--space-sm);
    padding: var(--space-lg);
    border-radius: var(--radius-md);
  }

  .policy-checklist__card ul {
    display: grid;
    gap: var(--space-xs);
    padding-left: var(--space-lg);
    margin: 0;
  }

  .policy-toc {
    display: grid;
    gap: var(--space-lg);
    max-width: 640px;
  }

  .policy-toc__list {
    display: grid;
    gap: var(--space-sm);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .policy-toc__list a {
    color: inherit;
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }

  .policy-toc__list a:hover,
  .policy-toc__list a:focus {
    border-color: var(--color-primary);
  }

  .policy-detail__inner {
    display: grid;
    gap: var(--space-md);
    max-width: 840px;
  }

  .policy-detail__content {
    display: grid;
    gap: var(--space-md);
  }

  .policy-detail__content ul,
  .policy-detail__content ol {
    display: grid;
    gap: var(--space-sm);
    padding-left: var(--space-lg);
  }

  .policy-detail__content a {
    color: var(--color-primary);
  }

  .policy-detail__content a:hover,
  .policy-detail__content a:focus {
    text-decoration: underline;
  }
</style>
